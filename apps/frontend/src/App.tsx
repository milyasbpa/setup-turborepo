import { AppRouter } from '@/core/router/AppRouter';
import './App.css';

/**
 * Main App Component
 * Entry point that sets up routing for the entire application
 */
function App() {
  return <AppRouter />;
}

export default App;
